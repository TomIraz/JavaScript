const titulo = document.querySelector(".titulo");

titulo.setAttribute("contentEditable","true"); // editar contenido, verdadero o falso
titulo.setAttribute("dir","rtl"); // direccion, rigth to left

titulo.setAttribute("hidden","true") // esconder o no el selector

index.setAttribute("tabindex", "0") // nos permitira darle focus con tab o no y elegir el orden de cada tab en la pagina

subtexto.setAttribute("title","a re loco") // cambia el titulo por el "a re loco